import { ProfileAddressesPage } from "@/views/ProfilePage.jsx";

export const metadata = {
  title: "Addresses",
  robots: { index: false, follow: false },
};

export default function Page() {
  return (
    <ProfileAddressesPage />
  );
}
